
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This is a temporary rule for development.
    // It allows open access to your entire database.
    // We MUST implement proper security rules before deploying to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
