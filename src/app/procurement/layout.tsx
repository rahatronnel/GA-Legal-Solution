"use client";

// This layout can be used for any shared logic or context for all procurement modules.
export default function ProcurementLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
